#ifndef BASIC_H
#define BASIC_H

/**
 * Validates a loop that checks an array of validation flags.
 * @param size Number of flags to check
 * @return 1 if all flags are valid (1), 0 otherwise
 */
int validate_loop(int size);

#endif

